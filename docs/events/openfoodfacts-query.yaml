asyncapi: "3.0.0"

info:
  title: Events that off-query subscribes to
  version: 0.0.1

servers:
  redis:
    title: Redis server
    host: redis:6379
    protocol: redis

operations:
  product_updated:
    action: receive
    channel:
      messages:
        $ref: "https://raw.githubusercontent.com/openfoodfacts/openfoodfacts-server/refs/heads/main/docs/events/messages/product_updated.yaml"
